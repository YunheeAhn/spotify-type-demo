// Spotify API 인증 관련 설정
// 클라이언트 ID와 시크릿 키를 환경 변수에서 가져옴
export const clientId = import.meta.env.VITE_SPOTIFY_CLIENT_ID as string;
export const clientSecret = import.meta.env.VITE_SPOTIFY_CLIENT_SECRET as string;
